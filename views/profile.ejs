<!DOCTYPE html>
<html>

<head>
  <title>User Profile</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <div class="container">
    <div class="main-body">
      <div class="row gutters-sm">
        <div class="col-md-4 mb-3">
          <div class="card">
            <div class="card-body">
              <div class="d-flex flex-column align-items-center text-center">
                <img src="https://bootdey.com/img/Content/avatar/avatar7.png" alt="Admin" class="rounded-circle"
                  width="150">
                <div class="mt-3">
                  <h4>
                    <%= user.name %>
                  </h4>
                </div>
              </div>
            </div>
          </div>
        </div>
        <div class="col-md-8">
          <div class="card mb-3">
            <div class="card-body">
              <h3>
                <%= user.name %>'s Info:
              </h3>
              <% if (user.aboutMe) { %>
                <div class="form-group">
                  <label for="aboutMe">About Me:</label>
                  <p id="aboutMe">
                    <%= user.aboutMe %>
                      <form action="/profile/delete/aboutMe" method="POST" class="delete-form">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger delete-btn">Delete</button>
                      </form>
                  </p>
                </div>
                <% } %>
                  <% if (user.email) { %>
                    <div class="form-group">
                      <label for="email">Email:</label>
                      <p id="email">
                        <%= user.email %>
                          <form action="/profile/delete/email" method="POST" class="delete-form">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="btn btn-danger delete-btn">Delete</button>
                          </form>
                      </p>
                    </div>
                    <% } %>
                      <% if (user.website) { %>
                        <div class="form-group">
                          <label for="website">Website:</label>
                          <p id="website">
                            <%= user.website %>
                              <form action="/profile/delete/website" method="POST" class="delete-form">
                                <input type="hidden" name="_method" value="DELETE">
                                <button type="submit" class="btn btn-danger delete-btn">Delete</button>
                              </form>
                          </p>
                        </div>
                        <% } %>
                          <a href="/profile/edit" class="add-info-btn">Add more information</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>